# Pairing / Loopholes Audit — 2026-06-05

Read-only triage doc. ~60 findings across 3 buckets + 1 security flag bucket.
Concurrent admin session is in flight on `app/layout.tsx`, `lib/swr-config.ts`,
`lib/cache/localStorageAdapter.ts`, `lib/auth.tsx`, `lib/hooks.ts`,
`app/social/page.tsx` — those files are mentioned for context only,
**not in scope** here.

---

## 🚨 P0 (drop everything)

- [ ] **Notification preferences are a localStorage-only placebo** — `/Users/samc/Desktop/lionade/app/profile/page.tsx:1365-1418` (`NotificationsSection`) — 7 toggles (Daily Reminder, Duel Challenges, Weekly Report, Badge Unlocked, Streak at Risk, New Features, Marketing) write to `localStorage("notifPrefs")` only. There is NO `notification_preferences` table, no API, the email program ignores them, and the bell-icon Realtime channel doesn't read them. A user who turns off "Marketing" still gets marketing pings. Either persist server-side to a new `notification_preferences` table or remove the section.
- [ ] **Settings page notification + privacy toggles do nothing** — `/Users/samc/Desktop/lionade/app/settings/page.tsx:33-38` — Streak Reminders, Duel Invites, Leaderboard Updates, Public Profile, Show on Leaderboard are `useState` placeholders explicitly commented "not in DB yet — placeholders for future." No save call. Same fix as above, or delete.
- [ ] **Profile Privacy section toggles also localStorage-only placebo** — `/Users/samc/Desktop/lionade/app/profile/page.tsx:1063-1155` — Profile Visibility (public/friends/private), Show on Leaderboard, Show Streak, Show Coins, Duel-challenges-from settings all save to `localStorage("privacySettings")`. Critical because users will assume "private profile" actually hides them — but `/api/social/search` returns them anyway. Privacy contract is broken.
- [ ] **"Delete Account" button is a dead button** — `/Users/samc/Desktop/lionade/app/profile/page.tsx:1267-1269` — no `onClick` handler. Just renders. Sitting in the "Danger Zone" with no behavior is worse than missing — implies intent and breaks trust. Either wire it up to a real flow (confirm + `supabase.auth.admin.deleteUser` via server route) or remove.
- [ ] **Subjects exists, /learn blocks it** — `/Users/samc/Desktop/lionade/app/learn/page.tsx:298-316` (button) + 608-619 (toast) — the dashboard's secondary-action row has a "Subjects · Coming Soon" gold-pill button that only fires a 3s toast, but the page `/learn/paths/page.tsx` AND `/learn/paths/[subject]/page.tsx` BOTH EXIST and are fully wired. Either route the button (the page is shipped) or fence-off the page route too. Right now the page is reachable from `router.push("/learn/paths")` elsewhere — inconsistent gate.

---

## P1 (next pass)

### Bucket A — Visual consistency (state shown as badge only, ripe for full-surface tint)

- [ ] **Shop item cards — rarity is a corner pill only** — `/Users/samc/Desktop/lionade/app/shop/page.tsx:493-514` (premium card), 580-621 (username effect), 1130-1170 (Today's Drops), 1196-1230 (Trending), 117-128 (Cosmetics) — every card uses the SAME uniform `linear-gradient(135deg, rgba(10,16,32,0.9), rgba(6,12,24,0.95))` background regardless of rarity. Rarity differentiation is ONLY in the border + tiny top-right pill. Highest-traffic surface where the WordBank/Sketchy treatment would land big. Fix: tint the background gradient using `RARITY_COLORS[r].badge` accent at low alpha (8-12%) so common/rare/epic/legendary read at a glance. Score: 2.
- [ ] **Badges page Earned grid — same problem** — `/Users/samc/Desktop/lionade/app/badges/page.tsx:91-93` — all earned badge cards use `linear-gradient(135deg, rgba(10,16,32,0.9), rgba(6,12,24,0.95))` — rarity only in border `s.border` + tiny text. `BadgeCard.tsx` (the component) is already on-pattern at line 30-36, but the badges PAGE re-implements its own card and discards the tint. Make `/app/badges/page.tsx` use `<BadgeCard />` and delete the duplicate card markup. Score: 2.
- [ ] **Leaderboard rows below rank 3 — uniform background** — `/Users/samc/Desktop/lionade/components/LeaderboardRow.tsx:48-57` — top-3 rows tint by rank color (gold/silver/bronze), but rows 4+ all share `border-electric/10 bg-navy-50`. Either tint by tier (`tier.color` from `getEloTier`) the way Social friend list does, or by Fangs/ELO percentile band. Score: 2.
- [ ] **Friend list rows — online dot + unread pill but uniform body** — `/Users/samc/Desktop/lionade/app/social/page.tsx:829-885` — state is communicated via tiny green dot + tiny gold pill; the row body is uniform. Online users could get a faint green wash, friends-with-unreads a faint gold wash. Score: 2. (Note: this file is in the other admin's worktree — flag as candidate only, don't ship in this sweep.)
- [ ] **Quiz difficulty selector — unselected cards all identical** — `/Users/samc/Desktop/lionade/app/quiz/page.tsx:712-738` — when a difficulty is SELECTED it tints by color; when UNSELECTED all three (Beginner / Intermediate / Advanced) share `linear-gradient(135deg, #0a1020 0%, #060c18 100%)`. Give each unselected card a 4-6% wash of its own color so the trio reads as green/yellow/red even at rest. Score: 1.
- [ ] **QuizCard difficulty pill but uniform card** — `/Users/samc/Desktop/lionade/components/QuizCard.tsx:162-166` — during a quiz, difficulty is a small top-right pill. The whole card content area is uniform. Faint difficulty wash on the question card border would do it. Score: 1.
- [ ] **Dashboard bounty cards — daily vs weekly look identical at idle** — `/Users/samc/Desktop/lionade/app/dashboard/page.tsx:723-748` (daily) + 769-793 (weekly) — both share `linear-gradient(135deg, #0d1528 0%, #0a1020 100%)`; only the top accent stripe color and section header distinguishes them. Tint daily gold-washed, weekly purple-washed. Score: 2.
- [ ] **Dashboard mission cards — uniform until completed** — `/Users/samc/Desktop/lionade/app/dashboard/page.tsx:488-494` — at idle, all three missions use `linear-gradient(135deg, #0d1528, #0a1020)`; only `completed`/`claimed` states tint. Use `${mission.color}08` wash at idle so the three mission types read distinctly. Score: 2.
- [ ] **Academia + Classes class cards — body is uniform, only thin top stripe carries class color** — `/Users/samc/Desktop/lionade/app/academia/page.tsx:277-322` + `/Users/samc/Desktop/lionade/app/classes/page.tsx:111-185` — card body is `bg-white/[0.03]`, the 1px color stripe is the only differentiator. Wash gradient using `cls.color` at 4-6%. Score: 2.
- [ ] **Party game-select cards — selected tints, unselected uniform** — `/Users/samc/Desktop/lionade/components/party/RoomLobby.tsx:485-495` — when NOT selected, Sketch/Bluff/PokerFace all share `linear-gradient(135deg, rgba(16,12,26,0.7), rgba(8,6,16,0.7))`. Use `${accent}08` wash even when unselected so the purple/gold/blue identity stays. Score: 1.
- [ ] **Notification dropdown rows — read state is a 2px left stripe only** — `/Users/samc/Desktop/lionade/components/Navbar.tsx:597-628` — `borderLeft: "2px solid #FFD700"` for unread vs transparent. The whole row could get a gold-washed background for unread items. Score: 1.
- [ ] **Stripe subscription status tone is text-only pill** — `/Users/samc/Desktop/lionade/app/account/page.tsx:259-265` + 70-76 — past_due / canceled / trialing / active is communicated only by a pill at the top of the subscription card. The CARD itself could carry the urgency wash (past_due → faint red, canceled → faint amber, trialing → faint blue). Score: 1.
- [ ] **Recent activity rows in profile — uniform regardless of activity type** — `/Users/samc/Desktop/lionade/app/profile/page.tsx:1302-1320` — icon changes (Sword for duel_win, MedalMilitary for badge_bonus, NotePencil for quiz), but every row uses `linear-gradient(135deg, #0a1020, #060c18)`. Could tint by activity type. Score: 1.
- [ ] **Mastery exam cards — only "mastered" tints; in-progress vs new look identical** — `/Users/samc/Desktop/lionade/app/learn/mastery/page.tsx:485-521` — `mastered` exams get gold gradient; everything else (active, brand-new, near-ready) shares `bg-white/[0.03]`. Tint by `overallDisplayPct` band (struggling/progress/ready). Score: 2.
- [ ] **Wallet transaction rows — type is left-accent stripe only** — `/Users/samc/Desktop/lionade/app/wallet/page.tsx:317-340` — `accent` is a 3px left bar; row body uniform at `rgba(255,255,255,0.015)`. Could be a subtle full-row wash. Score: 1.

### Bucket B — Dead ends

- [ ] **Native `window.alert()` for quiz error** — `/Users/samc/Desktop/lionade/app/quiz/page.tsx:493` — "No questions available for this subject + difficulty yet. Try another!" uses the OS alert dialog. Replace with `toastError`. The alert blocks the page, breaks reduce-motion, looks broken.
- [ ] **Native `window.alert()` for "Copied to clipboard"** — `/Users/samc/Desktop/lionade/app/quiz/page.tsx:1331` — same fix, use `toastSuccess("Copied to clipboard")`.
- [ ] **Native `window.alert()` in class archive error** — `/Users/samc/Desktop/lionade/app/classes/[id]/page.tsx:118` — `else alert(r.error || "Couldn't archive class.")` — should be `toastError(...)`. Other archive paths in the same file already use toasts (line 405).
- [ ] **Native `window.confirm()` for class archive + note archive** — `/Users/samc/Desktop/lionade/app/classes/[id]/page.tsx:115` + 401 — `confirm(...)` is OS-modal and breaks the page aesthetic. Replace with a styled ConfirmModal component.
- [ ] **Class "Edit details" menu item is a TODO** — `/Users/samc/Desktop/lionade/app/classes/[id]/page.tsx:152` — `onClick={() => { setMenuOpen(false); /* TODO edit modal */ }}` — the menu item is visible, clicking does nothing. PATCH endpoint already exists at `/api/classes/[id]:179`. Either ship the modal or hide the menu item.
- [ ] **Contact form has no "Send another" CTA** — `/Users/samc/Desktop/lionade/app/contact/page.tsx:81-91` — after submit, `sent` flips and the page shows a static "Message Sent!" card with no path back. User who wants to file a second ticket has to refresh the page. Add a "Send another" button that resets `sent` state.
- [ ] **Daily Bet "Coming Soon" prize pool on /compete** — `/Users/samc/Desktop/lionade/app/compete/page.tsx:121-145` — entire hero "MONTHLY COIN POOL" / "Top players will compete for a monthly prize pool. Details coming soon. Launching with ranked competitive season V2 — 2026." The hero is the dominant component above the fold but it's a placeholder. Either ship the prize pool or replace with a smaller informational strip.
- [ ] **Wallet "Redeem Rewards" widget is empty Coming Soon** — `/Users/samc/Desktop/lionade/app/wallet/page.tsx:269-287` — the LEFT rail's "REDEEM REWARDS" panel is dashed-border with "Coming Soon" text and a "Visit the Shop" button as escape valve. The escape valve works, but the panel takes 1/2 the column for no functional content. Either ship Redeem or collapse the panel.
- [ ] **Shop Premium "Coming Soon" stranded buttons** — `/Users/samc/Desktop/lionade/app/shop/page.tsx:508-510` + 1329-1331 — premium cards have a disabled-state purple "Coming Soon" button with no aria-disabled, no tooltip, no waitlist sign-up. User who wants the item has zero next step. Add "Notify me" → mailchimp/waitlist hookup, or remove the cards entirely until ready.
- [ ] **Streak Revive cash path returns toast "coming soon"** — `/Users/samc/Desktop/lionade/components/StreakReviveBanner.tsx:71-76` (UI) + `/Users/samc/Desktop/lionade/app/api/streak-revive/route.ts:121-128` (server) — the $0.99 "Save with cash" button is rendered alongside the Fangs button but tapping it just fires `toastInfo("Cash purchases land with our Stripe rollout. Use Fangs for now.")`. The button looks live but isn't. Either ship the Stripe path or hide the cash button entirely until then.
- [ ] **Tournament placeholder on /compete** — `/Users/samc/Desktop/lionade/app/compete/page.tsx:135-144` — already counted above (same hero).
- [ ] **Leaderboard search/jump-to-rank missing** — `/Users/samc/Desktop/lionade/app/leaderboard/page.tsx:80-300+` — page renders top 200 + the user's anchored row, but there's no search/find. If you're rank 178 you have to scroll. Add a "Jump to me" button + username search.
- [ ] **Daily-quiz Subjects "Coming Soon" toast is fire-and-forget** — `/Users/samc/Desktop/lionade/app/learn/page.tsx:608-619` — toast appears for 3s then evaporates with no follow-up. No waitlist for the feature, no "notify when ready," no roadmap link. The toast is the entire dead-end. Either remove the gate (the page exists) or add a "Notify me when ready" CTA.
- [ ] **Avatar Picker has 4 DiceBear styles named "Coming"-ish but no fenced affordance** — verify quickly: the avatar picker section (profile page line 73-83 lists 10 styles) — confirm all 10 actually return images. (Lower priority — not a definite finding, just an audit check.)

### Bucket C — Missing counterparts

- [ ] **Class edit endpoint exists but no UI** — server has `PATCH /api/classes/[id]` (route.ts:179) but the only UI affordances are the TODO comment above and archive. Editing class name/color/emoji/professor/term is impossible from the UI even though the API supports it.
- [ ] **No "search by name" on Friend Search lacks a way to clear pending requests in bulk** — `/Users/samc/Desktop/lionade/app/social/page.tsx` Add-Friend popover has per-row Undo, but no "Undo all" for users who sent many.
- [ ] **Badges page has zero filtering / sorting** — `/Users/samc/Desktop/lionade/app/badges/page.tsx:50-129` — only an Earned/Locked split. No rarity filter, no search, no sort. With 8+ badges it's already a wall.
- [ ] **Notification trigger / Settings disconnect** — see P0. Notifications fire from many places (`StreakReviveBanner`, friend Realtime, dashboard claims), but the user has no opt-out persisted server-side.
- [ ] **Fang IAP packs have a Buy path but no refund / dispute help** — `/Users/samc/Desktop/lionade/app/shop/page.tsx:740-815` — no link to "Need a refund? Contact support" near the buy button, no terms link. Stripe handles refunds at the dashboard level but the UX doesn't surface it.
- [ ] **Subscription "Manage" goes to Stripe Portal — no inline cancel** — `/Users/samc/Desktop/lionade/app/account/page.tsx:302-330` — design choice, but document somewhere that the in-app cancel intentionally redirects out. Currently the only signal is the helper text "cancel anytime in the Stripe Customer Portal." Acceptable, flag for review.
- [ ] **Friend "Block" missing** — `/Users/samc/Desktop/lionade/app/social/page.tsx` — Add/Accept/Decline/Remove are wired, but blocking isn't a concept. Privacy settings imply harassment vectors exist (`Allow Duel Challenges From: nobody`), but block-user is the actual primitive.
- [ ] **Shop equip without unequip** — `/api/me/equip` + `/api/shop/equip` exist; UI lets you swap one cosmetic for another but there's no "remove this frame, go back to no frame." Either acceptable design or missing CTA.
- [ ] **Settings page has "Email" displayed but no change-email UI** — `/Users/samc/Desktop/lionade/app/settings/page.tsx:125-131` — email is shown as plain text. Change is impossible without Supabase admin or going through support@. Add a Change Email row → Supabase `auth.updateUser({ email })`.
- [ ] **Settings vs Profile section drift** — Settings page (`/app/settings/page.tsx`) has its own 5 toggles for notif + privacy; Profile (`/app/profile/page.tsx`) has its own 7 + 5. They are SEPARATE state stores (Settings local React state lost on navigate, Profile localStorage). They should share one source. Drift hazard if both ship before consolidation.
- [ ] **Notification dropdown shows toasts but no "Mark all read" button** — `/Users/samc/Desktop/lionade/components/Navbar.tsx:580-630` — opening the panel auto-marks-all-read at line 261, so the button isn't strictly missing. But if a user wants to keep some unread to come back to, there's no per-row mark-read. Mark-as-read API supports per-id since notifications return `id`.
- [ ] **Subscription page (`/app/settings/subscription/page.tsx`) is a separate route from `/account`** — both manage the subscription, but `/account` came first and `/settings/subscription` was added later. Likely orphan or duplicate. Verify before letting another feature link to either.
- [ ] **Sketchy/Bluff post-game flow has no rematch CTA** — verify via `/Users/samc/Desktop/lionade/components/party/SketchView.tsx` — at round-end reveal, players see scoreboard + factoid but no "Play again with same group" button. Have to leave the room and re-join.
- [ ] **PartyRoomLobby has `MidGameInviteModal` (room-late-joiner support) but the lobby has no "Invite friend" twin from the LOBBY** — verify `/Users/samc/Desktop/lionade/components/party/RoomLobby.tsx` — RoomCodeShare exists for the code, but the friend-graph (Social tab friends) isn't surfaced as a direct invite list.
- [ ] **Vocab bank `is_public` toggle in BankSelector** — `/Users/samc/Desktop/lionade/components/Vocab/BankSelector.tsx:115` — flip exists, but Discover tab UI doesn't show "this is YOUR public bank" badge to the owner. Owner can publish but can't see it as published from their POV.
- [ ] **No "delete word" UI surfaced beyond VocabList row hover** — confirm: VocabList:139 calls apiDelete on words. Likely fine; flag for review.
- [ ] **Daily Bet has place-bet but no "cancel bet" before quiz starts** — `/api/place-bet` only writes; once placed, the user is committed. Acceptable for a betting product but worth flagging.

---

## P2 (nice to have)

- [ ] **Shop "BUY FANGS" packs use accent="mega" / "value" pill but background is uniform** — `/Users/samc/Desktop/lionade/app/shop/page.tsx:752-758` — Mega and Value packs do tint background; standard packs all share the same dark gradient. Already half-on-pattern, finish the trio.
- [ ] **Recent Activity panel in profile uses one icon per type but accent could deepen** — already noted, low priority.
- [ ] **Pricing FAQ uses a borderless accordion list** — `/Users/samc/Desktop/lionade/app/pricing/page.tsx:38+` — visual is fine, but answers and questions don't tint by topic.
- [ ] **Founder badge card has a sold-out overlay but no "join waitlist" CTA when sold** — `/Users/samc/Desktop/lionade/app/shop/page.tsx:644-650`.
- [ ] **Avatar "Adventurer" preset selector has uniform tiles** — `/Users/samc/Desktop/lionade/app/profile/page.tsx:89-123` — could subtly tint each preset tile.
- [ ] **Mastery `LimitPaywall` modal** — verify it has ESC + backdrop-click dismiss (called from line 391 in mastery page). If not, P1.
- [ ] **CreateClassModal at `/classes` page** — verify ESC + backdrop dismiss.
- [ ] **Dashboard's "Daily Bet" empty-state-after-loss text says "Place another bet below"** — `/app/dashboard/page.tsx:611` — fine, but consider a "Lost X / lifetime X" small stat to add context.
- [ ] **`/api/notifications` only supports mark-all-read (PATCH)** — no per-id mark-read or mark-unread. Symmetry gap if Bucket C #11 is ever shipped.

---

## 🚨 Bucket D — Security flags stumbled on during the sweep

Not in scope for deep audit; surfacing only what I tripped over.

- [ ] **Profile "Privacy" toggles broadcast a privacy promise the server doesn't keep** — see P0 Profile Privacy entry above. "Profile Visibility: private" stored in localStorage means `/api/social/search` (search) and `/api/social/feed` (activity feed) STILL return the user. This is a TRUST gap, not just a UX one — users believe they are private and are not. The server has no `profile_visibility` column to enforce against. **Flag this as P0 if any of those toggles ever shipped as a feature claim.**
- [ ] **`/api/notifications` GET / PATCH route uses `requireAuth` correctly** — confirmed clean.
- [ ] **`/api/streak-revive` cash path returns a `coming_soon` response but the body shape is consistent** — confirmed safe; no info leak.
- [ ] **`/api/contact` and `/api/waitlist` are intentionally unauthenticated** (public form endpoints). Confirmed they don't take user_id from body.
- [ ] **Settings page is the only place that exposes the user's email** — `/app/settings/page.tsx:128` — `{user?.email ?? "—"}` — this is fine, but if any cosmetic/avatar route ever included email in its payload (it doesn't, confirmed), the leak surface grows. Note for future.

---

## Out of scope (flagged but not in this sweep)

- iOS gaps — tracked in `IOS_PARITY.md`, web-only this session.
- The 6 files the concurrent admin owns (`app/layout.tsx`, `lib/swr-config.ts`, `lib/cache/localStorageAdapter.ts`, `lib/auth.tsx`, `lib/hooks.ts`, `app/social/page.tsx`). Social findings (friend row tinting, etc.) are listed for visibility but should land AFTER the other session's social cold-load fix.
- Server-side refactors (e.g. consolidating `/app/account` and `/app/settings/subscription`) — out of scope unless they bleed into UX.
- SEO / robots / sitemap audit — separate concern.
- Performance audit — not part of this triage.
- Accessibility deep audit — separate sweep; this audit flagged only the alert()/confirm() dialogs since they break aesthetics.

---

## Triage suggestion

Start with P0 #1-#4 (settings + privacy + delete-account integrity) because all four are TRUST issues — users believe state is saved when it isn't. P0 #5 (Subjects gate vs Subjects page) is a one-line decision (drop gate or fence page). Then Bucket A #1 + #2 (shop + badges) for the highest-visibility tint upgrade — same recipe as Word Banks, will land in a couple hours. Bucket B alerts (3 findings) take 15 minutes each; queue them when context is free.
