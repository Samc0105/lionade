# Feature Registry

## Auth & Onboarding
- **Email/password + Google OAuth login/signup** — 2026-02-19
- **Sign in with Apple (web)** (`/login` Log In + Sign Up tabs both show a black "Continue with Apple" button beneath the Google button; `supabase.auth.signInWithOAuth({ provider: 'apple' })` mirroring iOS; redirect-back to current origin like the Google flow) — 2026-05-24
- **Multi-step onboarding flow** — 2026-02-24
- **Session expiry + brute force protection** — 2026-02-20
- **ProtectedRoute with self-healing profile rows** — 2026-02-24
- **Auto logout after 2 hours of inactivity** — 2026-03-17

## Dashboard
- **Command center layout** (hero header, stat pills, XP bar, mission hero, continue shelf, Ninny's Notes) — 2026-02-28
- **Daily Claim — single-click + faster toast** (the daily-Fangs button: clicking when available now claims **instantly** without auto-opening the history popover; cooldown/claimed state still opens the popover on click. Reveal toast auto-dismiss reduced from 5s → 3s for tighter attention). `components/ClockInButton.tsx` — 2026-05-24
- **Dynamic greeting by time of day** — 2026-02-28
- **Idle glow animations on mission card and Ninny's Notes** — 2026-02-28
- **CTA pulse on "Start Daily Quiz"** — 2026-02-28
- **Reusable `ClaimBanner` + Free→Pro upgrade nudge** (one themeable banner across DailyReady/StreakRevive/ClockIn/DailyDrill claim surfaces; free-tier-only Pro nudge via `usePlan()`, links to /pricing) — 2026-05-17
- **Achievement grid with earned/locked states** — 2026-02-28
- **Streak fire animation with motivational banner** — 2026-02-28
- **Continue shelf with real quiz history** — 2026-02-28
- **Dashboard void fixes (new-user first run)** (YOUR SUBJECTS empty card + Start-your-first-quiz CTA replacing an orphaned heading; Today's Missions now separates loading/empty/error instead of an infinite skeleton; Weekly Activity all-zero week shows a take-a-quiz nudge instead of a 240px zero-bar chart with "0% accuracy"; Fangs/Streak/Subjects stat circles + XP bar gated on `statsReady` with em-dash placeholders so the `coins:0` auth seed never flashes) — 2026-06-09
- **Bucket A visual-consistency sweep** — 5 surfaces unified to the "card body carries its own identity tint" pattern already shipped on Word Banks (2026-06-05) / Sketchy picker (2026-06-05) / Shop (2026-06-05). `/badges` Earned + Locked grids switched to the shared `<BadgeCard />` (rarity-tinted body); Dashboard bounty cards tinted gold (daily) / purple (weekly); Dashboard mission cards tinted by `mission.color` in idle state; `/leaderboard` rows 4+ tinted by Elo tier (Bronze/Silver/Gold/Platinum/Diamond); Academia + Classes class cards washed by `cls.color`. — 2026-06-05

## Learn
- **Circle/bubble UI with 4 options** (Daily Quiz, Subjects, Practice Sets, Study With Ninny) — 2026-02-28
- **Primary action hierarchy** (Daily Quiz bubble larger) — 2026-02-28
- **Hover glow/scale effects, idle float animations** — 2026-02-28
- **Coming soon modals for unreleased features** — 2026-02-28
- **Subjects CTA → Coming Soon block** (the `/learn/paths` Subjects card on `/learn` is now click-blocked with a "Coming Soon" gold pill on the card + a 3-second auto-dismissing toast that says "Subjects & Learning Paths — in development, coming soon"; Daily Quiz / Study with Ninny / Mastery Mode CTAs remain clickable) — 2026-05-24

## Word Banks (Vocab)
- **Word Banks V1-V3A** (add word with translation/definition, SM-2 spaced repetition review, language streak, bank kinds language/general, Discover + clone + publish, per-word translation/definition AI lookup) — 2026-06-03
- **Excel-style list redesign** (Tab C dense view: inline-add row, 2px confidence-color left stripe, click-to-cycle confidence dot with sticky user override, per-row magnifier popover with cached AI/dictionary lookup, filter chips All/Locked in/Shaky/Struggling/New, self_confidence column + PATCH endpoint, race-safe revision counter) — 2026-06-05

## Quiz System
- **Timed daily quiz flow with scoring** — 2026-02-27
- **Supabase session persistence** — 2026-02-28
- **Subject-based question routing** — 2026-02-27
- **Difficulty selector cards + answer explanations** — 2026-02-25
- **Anti-cheat wiring** — 2026-02-27
- **Coin burst animation on results** — 2026-02-28
- **Live missions & bet visibility during quiz (V1)** (floating pill on `/quiz` opens a bottom sheet showing Today's Missions + active Daily Bet with progress that ticks up live as the player answers. Pill joins the existing FocusMusic / QuickNote / LockIn float family; shared SWR cache with the Dashboard via `dashboard-missions/${uid}` and `dashboard-active-bet/${uid}`, mutated after every successful answer. New `components/ui/BottomSheet.tsx` framer-motion primitive with Esc / click-outside / swipe-down dismiss, focus-trap, ARIA modal, reduce-motion gate. Pinning deferred to V2.). 2026-05-26

## Arena / Compete
- **2v2 duo create/join UI** (commit `73e2de0`; web-only, zero AI. The arena hub 2v2 toggle was a "coming" placeholder; it now offers Create duo (mints a 4-digit code to share), Join duo (enter a teammate's code), or Solo (auto-pair). Two friends queueing 2v2 with the same `partyCode` land on the same team. Backend matchmaking already grouped by `party_code`, so this is the missing UI layer; the 1v1 flow + the stale-closure 45s timeout are untouched. Reviewer APPROVED) — 2026-06-10
- **Networked round start (lockstep countdown)** (commit `73e2de0`; web-only, zero AI. Each client used to run its own 3-2-1-GO countdown on mount, so clock skew + load time gave one player a head start. `competitive_matches` now carries a server-set `starts_at` (= `created_at` + 4.5s lead, migration `059` applied to prod); the Countdown anchors to it and each mode screen anchors round 1's clock to `starts_at` so both clients begin in lockstep. A late loader clamps to now (disadvantages only the late player, no edge); falls back to the local sequence when `starts_at` is null. Reviewer APPROVED; per-round full sync deferred. A server-authoritative Sabotage-attack version was built the same pass but code-review BLOCKED and reverted it — NOT shipped, logged as tech debt) — 2026-06-10
- **Competitive ELO-integrity gate + connectivity + forfeit/void** (commit `ff0d373`; web-only, zero AI cost. Enforces "you only earn ELO if you actually play against someone." New `lib/competitive/settle.ts` `settleClaimedMatch` is the single shared settle funnel for `/complete`, `/forfeit`, and the reaper: ELO + Fangs settle ONLY when BOTH teams recorded `>=1 competitive_response`; a no-show / instant disconnect VOIDS the match (status `voided`) with no `profiles` ELO/Fang write (`elo_after = elo_before`, zero Fang delta, no penalty to the player who showed), while a mid-match quit where both played settles normally. New `POST /api/competitive/match/[id]/forfeit` (participant-only) voids with no penalty if the opponent never engaged else forces the caller's loss and settles, sharing the atomic `active`->`completing` claim with `/complete` so it can't double-fire. New `/api/cron/reap-stale-competitive` (CRON_SECRET bearer auth, `vercel.json` cron every 3 min) resolves hung/AFK matches through the same gate. Migration `058` adds `voided` + `forfeited` statuses + a `forfeited_by` column to `competitive_matches`, applied to prod. Connectivity: `use-match-channel` reconnects via `subscribeResilient` + Supabase Presence, exposing `connection` / `opponentPresent` / `opponentLastSeen` with the gameplay protocol + 4 mode screens unchanged. UI: a Reconnecting banner, opponent-disconnected panel (End Match which voids if the opponent never played, or Keep Waiting), an explicit Forfeit, and VOIDED ("no Elo or Fangs changed") + FORFEITED result states in `ResultCard`/`useSettle`. Reviewer APPROVED, gate is unbypassable (all 3 settle paths funnel through one function; void is a true `profiles` no-op). Known debt: a crash in the sub-second settle window can strand a match in `completing`; a race-free recovery needs an `entered_completing_at` timestamp, deferred non-blocking) — 2026-06-10
- **Compete tier card wired to real ELO + honest matchmaking timeouts** (removed hardcoded `CURRENT_TIER_INDEX=8`: tier now computed from real arena ELO so new users start at Bronze with locked tiers above; `TIERS` ranges converted from win counts to ELO thresholds; invalid CSS color `"legend"` fixed; static `HEX_STATS` wired to live data via `/api/me/elo-rank` + `profiles.arena_wins` with the fake "Win Streak" stat removed and real placement progress N/5 career matches; arena hub 45s no-opponents timeout fixed (stale closure made it unreachable, leaving infinite SEARCHING); duel matchmaking gained a 60s timeout with honest no-opponents dead-end + dequeue; a 0-question duel renders an error card instead of a blank page) — 2026-06-09
- **IA consolidation — one Arena, V2 killed, /duel killed** (web-only, staged not committed; iOS paused. Ended the "Arena = 3+ confusing routes" problem. **Arena V1 folded in:** the real 1v1 ELO quiz duel moved `app/arena/page.tsx` → `app/compete/arena/duel/page.tsx` and is now the "Quiz Duel" mode — a 5th gold tile on the `/compete/arena` hub (tagged "Remote OK") alongside Sabotage, Zoom, Spectrum, Map Pin; its `arena_elo` ladder + `/api/arena/*` endpoints are UNCHANGED. **Arena V2 (dark ghost-replay) deleted:** `app/arena/v2/page.tsx`, all 6 `/api/arena/v2/*` routes, all 5 `components/arena-v2/*`, and `lib/arena-v2/{anon-handle,feature-flag,ghost-matcher}.ts`; the `NEXT_PUBLIC_ARENA_V2_ENABLED` flag is now dead. The live shared loss-cap (`lib/arena-v2/loss-cap.ts`) was NOT V2-only so it was preserved by moving to `lib/competitive/loss-cap.ts` with the one live competitive import repointed. Orphaned migrations `20260526213500_arena_v2_ghosts.sql` + `20260526220000_arena_v2_ghost_elo.sql` left in place (not dropped). **Legacy fake-bot `/duel` deleted** (simulated bot wagering coins — a trust hole). **Coherence fixes:** `/compete` H1 "ARENA" → "COMPETE"; "Games" nav tab → "Arcade" to match the page H1; `/leaderboard` now has tabs for all three ranked ladders — Quiz Duel (`arena_elo`), Competitive (`competitive_elo`), Squad (`squad_elo`) — plus Weekly Fangs, via new `getLadderLeaderboard()`. **Redirects** (307, query-preserving): `/arena` → `/compete/arena/duel`, `/arena/v2` → `/compete/arena`, `/duel` → `/compete/arena`. "Duel" now means exactly one thing. iOS: this is web routing/IA; V2 never had an iOS counterpart.) — 2026-05-28
- **Rank summary strip + 3 mode tiles** (Duel, Blitz, Leaderboard) — 2026-02-20
- **Weekly Tournament placeholder** — 2026-02-20
- **Full 1v1 Duel Arena with competitive battles** — 2026-03-20
- **Active-session toast (ResumeBanner replacement)** (`components/ActiveSessionToast.tsx` replaces the deleted sticky full-width ResumeBanner for all 5 active-session types: slides in from the right, party sessions show "You have an active party" + Rejoin + room code, 5s auto-vanish that re-arms on navigation; explicit Dismiss is sessionStorage-scoped and no longer deletes the server `active_session` pointer) — 2026-06-10
- **Poker Face both-sides UX** (presenter decide beat with the secret fact centered + I'LL SAY TRUE / I'LL SAY LIE server-side lock, 30s window from server timestamps, client TRUE auto-lock backstop that only banners after the POST commits; sell-it screen with no TRUE/LIE labels + I'VE READ IT, with the open-vote route now authorizing the presenter; caller waiting screen with presenter avatar + listen rings; BELIEVE/DOUBT 15s window with locked-in state; 2s dramatic pause + 3D card-flip reveal with per-caller results; ROUND N/M rotation header; always-visible score ticker) — 2026-06-10
- **Lionade Party — Trivia (Lightning Round), 4th party game** (commit `70d7016`; web-only, committed not pushed, zero new API cost. A Kahoot-style MCQ race ADDED to the Party suite alongside Sketchy / Bluff / Poker Face — Bluff stays fully present, this is an addition not a replacement. Code id `trivia`, display name "Lightning Round". Flow: lobby pick, a 5s countdown, a ~12s answer phase where everyone taps one of 4 options as a single immutable pick, a ~6s reveal where the correct option turns green and points = base 500 + speed bonus + streak bonus with the leaderboard re-sorting, then the next round, then a Game Over podium after N rounds (default 10). Server-authoritative phase timers (`trivia_rounds.answer_ends_at` / `reveal_ends_at`) with a compare-and-swap phase advance + a GET-route lazy-advance + an any-member fallback so a backgrounded host can't freeze the room; an "everyone answered early" check shortens the answer deadline. Questions reuse the existing free OpenTriviaDB feed + curated fallback bank. New `components/party/TriviaView.tsx` + `components/party/AvatarCheckRow.tsx` (extracted from `BluffView`), `lib/party/trivia-questions.ts`, `lib/party/trivia-advance.ts`, and `app/api/party/trivia/rounds` route + `[id]/{route,answer,complete}`. Rematch + start routes delete prior `trivia_rounds` so per-match round numbering resets (fixes a rematch -> instant-Game-Over bug). A11y: reveal correctness announced via a single assertive live region, reduced-motion gating, 44px touch targets. Migrations `lib/migrations/061_party_trivia.sql` (trivia tables + RLS/FORCE + realtime + a column REVOKE of `correct_index`) AND `062_party_secret_column_revoke.sql` (parity REVOKE of `bluff_rounds.correct_answer` + `sketch_rounds.word` + FORCE RLS) are BOTH NOT YET APPLIED — Sam runs both manually; 061 before Trivia works, 062 to close the Bluff/Sketch answer-leak. *(Updated 2026-06-11, commits `3412f41` + `4f32429`: Trivia gained **host lobby controls** in `RoomLobby.tsx` — question count 5/10/15 + answer speed Chill (20s) / Normal (12s) / Blitz (8s), sent in the start payload.)*) — 2026-06-11
- **Party resilience + a11y follow-ups (Sketchy/Poker Face lazy-advance, reveal-announcer fix)** (commits `3412f41` + `4f32429`; web-only, committed not pushed, no migration. **Server-authoritative phase resilience extended to all party games:** new `lib/party/sketch-advance.ts` (Sketchy lazy-complete via the guess route plus a new GET) and `lib/party/pokerface-advance.ts` (Poker Face GET lazy-advance), so a backgrounded host can no longer freeze Sketchy or Poker Face — Bluff + Trivia already had this. A latent Sketchy double-score window was closed with a CAS guard. **Accessibility sweep on `BluffView` + `PokerFaceView`:** both had an `aria-live` poll-spam bug where the reveal container re-announced on every 1.5s poll, replaced with a single per-round assertive announcer; added reduced-motion gating on loaders, 44px touch targets, and screen-reader reveal-outcome text. Poker Face secret columns were already whole-table REVOKE'd by migration `056`, so no new security migration was needed there) — 2026-06-11
- **Party live-playtest hardening** (Sam playtested with real players; 6 web-only commits, no migration. **Invites:** the in-room membership check did `.select("id")` on `party_room_players`, which has a composite PK `(room_id, user_id)` and no `id` column, so PostgREST errored and the swallowed error made every invite (host included) return "Join the room before inviting friends"; fixed by selecting a real column + `limit(1)` + error capture, an upsert fresh-join, an idempotent auto-join, plus an invite-delivery broadcast on `notifs-<friendId>` re-emitted to the invite bus and a restyled right-side 5s `PartyInviteToast`. **Bluff repair:** 8 breaks fixed via compare-and-swap phase transitions (only the CAS winner scores, reveal terminal), a `from_phase` stale-intent guard, `adoptRound` + seen-set, a single per-room subscription, any-member advance after deadline+grace, and create idempotency + an 8s loading-rescue retry. **Sketchy:** the "overlay blocking the canvas" was the word-blanks mask pill moved back into normal flow; guesses are now panel-only (correct = green "[name] got it! ✓" with the word hidden from non-guessers, wrong = muted gray); the canvas widened to fill viewport height (`max-w-7xl`, dvh-sized at the 1000x600 logical space) with the guesses panel at 260px; a new 16-color 4x4 swatch palette with a cream selected ring; the canvas surface flipped near-black -> paper `#EEF4FF` so the palette's dark inks are visible; round 1 (which was dead because the START raced clients' channel subscribe) fixed with the BluffView adoption pattern (shared `adoptRound` + seen-set + 3s snapshot poll carrying a `word_picked` boolean, drawer `/words` fetch on every adoption path, host auto-start gated on SUBSCRIBED with a 4s fallback); and a word-leak fix where a near-miss "close" guess lit the whole word green, now bounded server-authoritatively by a hidden floor of 1/3 of letter cells (min 1) that guesses can never reveal. **Connectivity:** supabase-js dedupes channels by topic with an async leave, so `page.tsx` + `RoomLobby` shared one `party-room-{code}` instance and RoomLobby's unmount killed the page channel mid-game; the page is now the sole channel owner created once, players `postgres_changes` moved to a dedicated `party-room-{code}-players` topic, RoomLobby attaches via prop, `PokerFaceView` ported to single-subscription + `adoptRound`, all sends are get-or-create-never-remove, the straggler nudge uses `subscribeResilient`, tab-close broadcasts PLAYER_LEFT server-side, and a `visibilitychange` refresh was added so every game transition is a fast-path broadcast on a resilient channel with the 3s poll as reconciler) — 2026-06-11
- **Shared party round components** (`components/party/RoundCountdown.tsx` 5s between-rounds overlay + `components/party/GameOverScreen.tsx` podium scoreboard with Fang-payout slot, Play Again via rematch, Back to Lobby; consumed by both Poker Face and Bluff Trivia) — 2026-06-10
- **Word Bank → Sketchy V1** (players opt one of their own `/learn/vocab` Word Banks into a Sketchy game as a word source; the bank rides in the per-player `selected_subjects` array as a `bank:<uuid>` token sharing the 2-pick cap with curated subjects, and each round draws one source so banks mix with curated subjects round to round; gate is owned-by-caller + `>=30` words (`MIN_BANK_WORDS`); grey "Word Bank" button in the YOUR TOPICS picker opens a BottomSheet via `GET /api/party/rooms/[code]/banks` listing the caller's banks with counts + eligibility (under-30 greyed `N/30`); bank candidates get a grey "Bank" pill + the word's definition as a card subline, the drawer gets one reroll per round via `POST /sketch/rounds/[id]/reroll`, reveal shows the definition under a DEFINITION eyebrow, and other players' bank picks render as an anonymous "Word Bank xN" so private bank names never leak; migration `supabase/migrations/20260610_sketch_bank_source.sql` adds `sketch_rounds.source_kind`/`source_bank_id`/`rerolled` (additive, defaulted), NOT yet applied to prod; zero API cost; full spec `docs/specs/word-bank-sketchy.md`) — 2026-06-10
- **Sketchy word bank expanded to 1404 words** (the Pictionary pool nearly doubled from 752: 652 new drawable words added across all 8 subjects, each with a true one-sentence factoid and an easy/medium/hard tier, deduped case-insensitively per subject and validated before merge. Every subject now has a full spread across all three tiers so the tiered candidate picker rarely falls back; thinnest hard tier went from 9 to 29. Source `lib/party/word-lists.ts`, re-seeded to `party_word_lists`, row count 1404) — 2026-06-10
- **Sketchy round-flow parity** (Sketchy now consumes the shared `RoundCountdown` between rounds: 5s, ROUND N of M from live rotation math, replay-proof seen-set, pick timer + drawing clock held during the intro, `selectWord` guarded against overlay click-through; host-ended game over via new GAME_OVER broadcast: END GAME CTA on reveal routes everyone to the shared `GameOverScreen` podium with Play Again / Back to Lobby, auto-advance suppressed, handler ignores out-of-phase deliveries; staged ~3.9s reveal: celebrating overlay, gold word, factoid, staggered scoreboard with CountUp, then CTAs, reduced-motion all at once; canvas/header fade-slide per round; scoreboard staggerIn opt-in default-off so Bluff and Poker Face are unchanged; ROUND_STARTED now carries `round_num`) — 2026-06-10
- **Bluff Trivia polish + forfeit exploit fix** (host parity audited clean with identical payloads; forfeit-sentinel exploit fixed: it rendered as a votable card granting unearned +500s, now filtered server-side from vote + reveal with a vote-route backstop and a shared constant in `lib/party/bluff-constants.ts`; write phase with labeled input + avatar submit checkmarks, ids only; timeout = skip, since a client auto-submit of "..." races the host advance and corrupts dedup; vote phase with own-fake gray-out + locked-in + voted checkmarks via new ids-only `voted_user_ids` + `my_answer_id` on the round GET; one-by-one reveal with author avatars, fooled counts, gold TRUTH badge, per-player points breakdown, count-up scoreboard; write window padded +5s for the countdown; vote upsert errors no longer silently succeed. *(Updated 2026-06-11, commit `70d7016`: two live-playtest bugs fixed — "answers not saving" was an over-aggressive cross-player dedup that silently blocked similar/identical fakes, now removed, with a fake matching the real answer returning a clear UI message instead; "doesn't continue / freezes" fixed by extracting a shared `lib/party/bluff-advance.ts`, lazy-advancing a stale phase server-side from the GET route, and giving `isEffectiveHost` a `user_id` tiebreak so client and server agree on who advances. See the Trivia entry above for the paired secret-column REVOKE migration 062 that also closes a Bluff answer-leak.)* ) — 2026-06-10
- **Party instant invite loop** (global `components/party/PartyInviteToast.tsx` banner on every page when a `party_invite` notification lands: Join routes to the room + marks the notification read, Dismiss, 30s auto-dismiss with a draining bar; single-WebSocket design where the Navbar's existing notifications channel re-emits onto a window event bus at `lib/party/invite-bus.ts`; join + host-approve routes now broadcast PLAYER_JOINED so the host lobby updates live; lobby friend-invite list shows a persistent "Invited" state that flips to "In room" when the friend joins) — 2026-06-10
- **Party lobby animations + polish** (staggered skeleton wave on empty player slots at 180ms per slot, join slide-up + glow-bloom pop keyed off seen ids, ready players get the Social-tab green ring/glow via the optimistic path; new `pa-slot-wave` / `pa-join-in` / `pa-ready-lit` keyframes, reduced-motion guarded; copy icon + "Copied!" toast on the invite-link line; game tiles with icon/name/tagline/real player counts (2-6) + best-played badge + gold selected state; chat bubble chat icon + unread badge + attention pulse; READY UP green fill + check; N/M READY header with count tick; START pulses gold when all ready, host still taps) — 2026-06-10
- **Sketchy tiered word candidates + waiting card** (drawer candidates are now one easy + one medium + one hard with green/amber/red pills, gold border + scale on select while the others fade, nearest-tier fallbacks for thin pools; migration `supabase/migrations/20260610_party_word_difficulty.sql` adds the CHECK + char_length backfill on `party_word_lists.difficulty`, NOT yet applied to prod; non-drawers get a "Waiting for [drawer] to pick a word" purple card with pencil-write animation + 15s safety timeout instead of a blank canvas) — 2026-06-10
- **Party realtime performance + hardening pass** (READY_CHANGED client broadcast fired in parallel with the DB write, replacing an unfiltered table listener + snapshot GET with a server-filtered room listener; broadcast-first chat with client-generated uuid + de-dup + server backstop, rendered pending-until-authoritative so spoofable client broadcasts stay dimmed until the server-verified copy lands, 10s expiry drops ghosts; lobby-chat 23505 recovery ownership check closes a foreign-row adoption hole; join/invite query waves parallelized; round-start fast path with inline drawer candidates + route warming on tile select + idle chunk prefetch + early ROUND_STARTED; channel-leak fixes in `sendRoomEvent` + nudges; StrictMode-safe unread counting; invite-toast exit-timer race fixed) — 2026-06-10
- **Lionade Party V1 — multiplayer game suite at `/compete/party`** (web-only scope per CEO 2026-05-26; iOS paused. Two games on shared room infrastructure: **Sketchy Subjects** subject-locked Pictionary for 2–8 players with rotating drawer, 3-candidate-word selection, 90s round timer, HTML5 canvas with 8 colors + 3 brush sizes + eraser/undo/clear (no fill bucket / no text tool per anti-cheat), server-validated fuzzy guesses via Levenshtein distance ≤ 2 = "Close!", rank-based scoring 1000/600/400/200 + drawer bonus 100/correct guesser plus 200 if 80%+ guessed within first 30s, post-round factoid card; and **Bluff Trivia** Fibbage-style write-fake/vote/reveal for 3–8 players with Open Trivia DB question source (15-min in-memory cache + curated fallback bank), 45s write phase + 30s vote phase, server-shuffled answer presentation with deterministic seeding so order is stable across re-polls, 1000 points for picking the truth + 500 points per player tricked by your fake. **Migration 051** adds 9 tables (`party_rooms`, `party_room_players`, `sketch_rounds`, `sketch_strokes`, `sketch_guesses`, `bluff_rounds`, `bluff_answers`, `bluff_votes`, `party_word_lists`) all RLS-enabled with `auth.uid() IS NOT NULL` SELECT policies and service-role-only writes, 8 tables added to `supabase_realtime` publication. **Helpers** at `lib/party/`: room-code generator using `crypto.randomInt()` CSPRNG with no 0/O/1/I/L confusables, Wagner-Fischer Levenshtein, realtime channel name helpers, scoring formulas, Open Trivia DB integration with HTML-entity decode, inline word-list stub (10 prompts × 8 subjects with factoids — fallback only since the parallel word-curator agent finished `lib/party/word-lists.ts` with 50+ entries per subject (400 total), wired in via a `pickCandidatesForSubject` helper, and seeded into `party_word_lists` via the new `scripts/seed-party-words.ts` idempotent upsert script — row count = 400). **13 API endpoints** at `app/api/party/*` all `requireAuth`-gated with no body-trusted userIds: room CRUD + sketch round lifecycle (rotating-drawer pick, drawer-only candidate-word fetch, drawer-only stroke persistence with bandwidth caps MAX_BATCH=64 / MAX_POINTS_PER_STROKE=2048, late-joiner replay GET, server fuzzy-validated guesses with one-correct-per-player gate and redacted `[correct]` storage to prevent word-leak via DB read, drawer-or-host round complete) + bluff round lifecycle (server-fetched question with truth stored as a `bluff_answers` row with `is_truth=true`, phase-aware GET that filters is_truth/author until reveal, fake-answer write with fake≠truth normalize() validation and dup rejection, vote with own-fake rejection, host-only phase advance with score deltas applied on vote→reveal). **10 UI components** at `components/party/`: `RoomCodeShare`, `PartyScoreboard`, `NinnyHostBubble`, `SketchToolbar`, `SketchCanvas` (HTML5 canvas + Pointer Events for unified mouse/touch, normalized 1000×600 logical coordinate space so strokes reproduce identically on every viewer's screen regardless of viewport, 30Hz batched broadcasts via Supabase Realtime `payload` send for low-latency live drawing + parallel ~500ms HTTP-batched persistence to `sketch_strokes` for late-joiner replay, eraser as `__erase__` color string repainted in canvas bg color), `SketchView` (4-phase loading/select-word/drawing/reveal screen with drawer-no-chat anti-cheat and guesser chat + guess input + correct-guess lockout), `BluffView` (3-phase write/vote/reveal screen with host auto-advance on phase timeout), `RoomLobby` (player grid + game-select cards + Start CTA host-only with min-player validation), `SketchView` reveal banner with factoid + sorted scoreboard. **2 page routes**: `app/compete/party/page.tsx` (landing with Create/Join + expandable "How to play"), `app/compete/party/[code]/page.tsx` (room shell with auto-join on mount + 3-channel Realtime subscription + 3s safety-net polling + leave-room action + lobby/sketch/bluff/end-game branching). **Compete page integration**: new LIONADE PARTY hero card on `/compete` between BLITZ+LEADERBOARD and WEEKLY TOURNAMENT with glow-purple tilt-card + NEW ribbon. **BackButton update** for `/compete/party` + dynamic `/compete/party/[code]` route. **Fang economy**: ZERO Fang stakes in V1 — pure score-based rankings per locked spec; V2 may introduce optional Fang pot per room. Deferred to V1.5 / V2: optional Fang stakes, premium word packs as Pro perks, canvas backgrounds + replay export, music bingo (held to avoid copying Sam's friend's project), Image Royale + Fake AP + Drawful, full word-curator output integration, distributed candidate-word cache for multi-instance scale, iOS port.) — 2026-05-26
- **Lionade Party — Poker Face (3rd party game)** (moved out of the Competitive arena 2026-05-28 as a no-ELO, no-Fang social party game; web-only, iOS paused. N-player (3-8) presenter-rotation: server picks a rotating presenter per round and deals a secret fact card; presenter presents the TRUE fact or a self-authored LIE; the rest of the room calls BELIEVE or DOUBT; reveal + score; presenter rotates. **Scoring (pure points to `party_room_players.score`, server-authoritative):** presenter +500 per caller fooled, caller +400 per correct read. **"Best in person"** framing — the face is the tell, so there is NO confidence-wager mechanic (dropped from the old competitive duel); the same room-code connectivity as Sketch/Bluff is used so people sitting together can read each other. **Migration 056** adds `party_pokerface_rounds` + `party_pokerface_votes`, RLS-hardened like 055 (anon/authed SELECT REVOKED on both; rounds readable directly only at reveal/ended or by the presenter; votes own-row-only; secrets served via service-role only). **Reuses the 51 curated fact cards** (`lib/party/pokerface-cards.ts`). **Endpoints** `app/api/party/pokerface/{rounds, rounds/[id], rounds/[id]/present, rounds/[id]/call, rounds/[id]/complete}` — all `requireAuth`-gated, server picks the presenter + deals the card, truth-claim forced server-side, scores never client-trusted. **UI** `components/party/PokerFaceView.tsx` (electric-blue, present/vote/reveal, card-flip + reveal motion, gather-your-crew banner) + 3rd game-select card in `RoomLobby`. **"Best played" glass badges** on all party game-select cards: Sketchy = Either, Bluff = Remote OK, Poker Face = Best in person.) — 2026-05-28
- **Competitive Modes — unified PvP arena platform + 4 modes** (web-only per CEO 2026-05-28; iOS paused. **UPDATE 2026-05-28: Poker Face was MOVED to Lionade Party as a no-ELO/no-Fang social party game (see the Party entry above) — the competitive arena is now 4 modes (Sabotage, Zoom, Spectrum, Pin), all carrying a "Remote OK" best-played badge. The legacy `pokerface_hands` table from migration 054 is now unused but left in place.** Four modes each 1v1 + 2v2 on one shared platform: **Sabotage Trivia** real-time trivia duel where fast-correct answers charge an attack meter spent on peer-broadcast attacks (blur/scramble/drain/decoy/freeze/fog, costs tuned to ~3-5 attacks per 90s skilled match); **Zoom Reveal** image un-blurs over 15s, earlier correct guess scores more (Levenshtein fuzzy match), wrong guess locks the round; **Spectrum Slider** drag a slider mapped to [min,max] to estimate a value, distance-based partial credit; **Map Pin Drop** drop a Leaflet/OpenStreetMap pin, haversine distance scores. (Poker Face was the 5th mode but moved to Lionade Party 2026-05-28.) **Two new ELO ladders** `profiles.competitive_elo` (1v1) + `squad_elo` (2v2), K=32, legacy `arena_elo` untouched. **Locked Fang payouts** (Sabotage ±30, Zoom ±25, Spectrum/Pin ±20 + minted participation; 2v2 = 2/3 per-player; Poker Face = zero-sum prize pot + flat participation). **Shared 24h loss cap** across Arena + all competitive modes via extended `computeLossWindow`. **Migration 054** adds `competitive_matches` (unified) + `competitive_queue` + 6 per-mode tables, all RLS-enabled, 7 added to realtime publication. **Matchmaking** 1v1 ELO ±200 widening to ±400 after 30s; 2v2 friend-duos via 4-digit party-code OR solo auto-pair; NO BOTS, honest no-opponents dead-end. **Content (zero AI):** 51 Poker Face cards, 25 Zoom images (Wikimedia), 50 Spectrum prompts, 40 Pin places + REST Countries centroids; Sabotage uses Open Trivia DB + fallback. **Poker Face BOUNDED only:** stake [10/25/50] + one raise (<=2x, total <=250 + clamped to balance/loss-cap headroom); NO all-in/multi-raise/rake; copy is Stake/Prize/Challenge only; casino escalation left as GATED lawyer-required TODOs. **UI:** `/compete/arena` hub + `/compete/arena/[mode]/[matchId]` + GAME MODES section on `/compete`. **npm:** leaflet + react-leaflet@4 (React-18-pinned). `npx tsc --noEmit` clean; all endpoints 401 without auth.) — 2026-05-28
- **Arena V2 Phase 3 — ghost-owner offline ELO buffer + Trainer accuracy tune** (still flag-gated; migration 050 adds `profiles.pending_elo_change/pending_elo_summary/pending_wins/pending_losses/pending_draws`. `/api/arena/v2/complete` now writes a symmetric `-1 × live_delta` to the ghost owner's pending buffer for real-ghost matches; trainer matches stay outside the conservation pool. New endpoints `/api/arena/v2/ghost-elo-summary` GET + `/api/arena/v2/claim-ghost-elo` POST. New component `components/arena-v2/GhostEloCard.tsx` rendered at the top of the V2 lobby when `hasPending === true`. Trainer Ninny accuracy retuned 75/85/95% to 60/75/90% via `scripts/seed-trainer-ninny-ghosts.ts --reseed`; 24 trainer ghosts reseeded with avg `total_score` per band 563 / 713 / 863. Deferred to Phase 4: RPC-based atomic claim, ELO-drift telemetry, global notification surface outside V2 lobby.) — 2026-05-26
- **Arena V2 Phase 3 — ghost-owner offline ELO buffer + Trainer accuracy tune** (still flag-gated; migration 050 adds `profiles.pending_elo_change/pending_elo_summary/pending_wins/pending_losses/pending_draws`. `/api/arena/v2/complete` now writes a symmetric `-1 × live_delta` to the ghost owner's pending buffer for real-ghost matches; trainer matches stay outside the conservation pool. New endpoints `/api/arena/v2/ghost-elo-summary` GET + `/api/arena/v2/claim-ghost-elo` POST. New component `components/arena-v2/GhostEloCard.tsx` rendered at the top of the V2 lobby when `hasPending === true`. Trainer Ninny accuracy retuned 75/85/95% to 60/75/90% via `scripts/seed-trainer-ninny-ghosts.ts --reseed`; 24 trainer ghosts reseeded with avg `total_score` per band 563 / 713 / 863. Deferred to Phase 4: RPC-based atomic claim, ELO-drift telemetry, global notification surface outside V2 lobby.) — 2026-05-26
- **Arena V2 Phase 2A — UI surfaces on top of Phase 1 backend** (still flag-gated; `app/arena/v2/page.tsx` with lobby + 1.5s shimmer + honest no-ghost dead-end + 2.2s prematch reveal + HP-bar battle screen with combo/comeback multipliers + emoji reactions + results card. `components/arena-v2/{HpBar,EmojiReaction,ConsentModal,GhostIdentity}.tsx`. New `/api/arena/v2/consent` endpoint. V1 `/arena` untouched. Deferred to 2B: 3-loss intervention card, Settings → Privacy anonymity toggle, adult opt-in real-username, 2-layer audio, Pro perks) — 2026-05-26
- **Arena V2 Phase 1 — async ghost-duel backend foundation** (gated behind `NEXT_PUBLIC_ARENA_V2_ENABLED`; `duel_ghosts` table + `lib/arena-v2/` helpers + `/api/arena/v2/{queue,ghost-replay,complete}` + 30-ghost Trainer Ninny seed script. V1 untouched.) — 2026-05-26
- **Leaderboard page (weekly/all-time toggle)** — 2026-02-28
- **Idle tilt/pulse/shimmer animations** — 2026-02-28

## Social
- **Social tab with friends and messaging** — 2026-03-20
- **Live username search for adding friends** — 2026-03-20
- **Add-Friend popover** (the ADD FRIEND search + Sent Requests list moved out of the always-visible left sidebar into a modal triggered by a gold UserPlus icon button placed in the friends-search header row. Modal: MagnifyingGlass-prefixed search input + autocomplete dropdown + Sent Requests list with "Undo" buttons. Click-outside + X dismiss. Empty-state when no pending requests) — 2026-05-24
- **Near-realtime pending friend requests** (the Social page's friends/pending list no longer waits on its 10s poll: the Navbar's existing notifications Realtime INSERT channel now invalidates the `social-friends/<uid>` SWR key the instant a `friend_request` or `friend_accepted` notification lands, so the recipient's Accept option and the sender's accepted-friend update appear in ~1s. Poll tightened 10s → 5s as a safety net and `revalidateOnMount` added so a stale persisted SWR cache can't mask a request that arrived while the user was away. No migration — reuses the live notifications channel; `friendships` RLS untouched) — 2026-05-29
- **Search surfaces pending-request users with the right action** (the user search API stopped hiding anyone with an existing accepted/pending friendship — it previously excluded them entirely, so searching for someone who had sent you a request returned nothing. The route now returns every match except self, each annotated with a `relationship` field — `none | incoming | outgoing | friends` — plus a `friendshipId` for pending rows. The search dropdown renders per-result actions: **Add** (none), **Accept** (incoming — accepts the pending request right from search via the same accept action as the pending list), **Requested** (outgoing, disabled), **Friends** (accepted, disabled). Self is still excluded; relationship lookups stay auth-scoped; no user-enumeration regression) — 2026-05-29

## Games
- **Roardle fixes — physical keyboard + standard Wordle duplicate scoring** (web-only, staged not committed; iOS paused. Added a scoped `window` `keydown` listener so players can type with their computer keyboard — letters append to the input, Backspace deletes, Enter submits — active only while Roardle is the open game and the round isn't over, ignoring Cmd/Ctrl/Alt shortcuts and keystrokes in text fields, with cleanup on unmount/leave; the on-screen keyboard still works. Fixed letter feedback to the real Wordle duplicate rule via a two-pass `getRowStatuses` scorer: greens first, then yellows only up to each letter's remaining count in the answer, surplus duplicates gray; grid, keyboard-state, win/lose all use it. Tiles gained a non-color cue (aria-label + ●/◐/○ glyph). Mechanics only — Lionade's own science word bank + "Roardle" name kept, no NYT content.) — 2026-05-29
- **Games tab** with Roardle, Blitz Sprint, Flash Cards, Timeline Drop — 2026-03-22
- **PDF upload for study material** — 2026-03-22
- **Lion mascot with cursor tracking** — 2026-03-22

## Notifications
- **Real-time notifications system with bell icon** — 2026-03-20

## Email Program
- **Phase 1 — Shared template skeleton + 5 transactional emails + 3 Supabase Auth reference HTMLs** (`lib/emails/` — typed slot renderer, no `@react-email`. Cream/parchment + gold-dark CTA palette, table-based, inline styles, system fonts, 600px max width; survives Gmail/Apple Mail/Outlook Web. Templates: `waitlistConfirmation`, `contactForm`, `welcome`, `firstStreakDay`, `masteryStart`. Send-trigger wiring: waitlist + contact + firstStreakDay + masteryStart wired; welcome deferred to Phase 1.5 (no `/api/auth/callback` route exists). Dev preview at `/dev/emails` — gated on `NODE_ENV !== "production"`. Supabase Auth templates not auto-applied — paste manually per `lib/emails/supabase/README.md`. Phase 2 hook: `{{aiPersonalization}}` slot in every body template.) — 2026-05-25

## Shop & Wallet
- **Shop overhaul — cosmetics render + equipped-slots model + booster fixes + full reprice** (commits `3412f41` + `4f32429`; web-only, committed not pushed, zero AI cost. Fixes "you buy things and nothing triggers + everything's too cheap." **Cosmetics now render:** new `components/Avatar.tsx` draws the equipped frame + aura as overlays (avatar-stability safe, no layout shift), `lib/cosmetics/cosmetic-styles.ts` maps cosmetic ids to visual styles, `AnimatedUsername` gained name-color support, and the profile hero gained a banner strip; rendered across profile (self), leaderboard (incl. the default elo-ladder tab), social, and party scoreboard/lobby (other users). **Equipped-slots model:** equipped state is the source of truth on new `profiles` columns from migration `063` — `equipped_frame`, `equipped_name_color`, `equipped_banner`, `equipped_avatar_aura` (all nullable text, NULL = none, mirroring the existing `equipped_username_effect`); the list endpoints read them inline off each profile row rather than joining `user_inventory` per row. **Owned-contract fix:** `/api/cosmetics/owned` was returning the wrong shape so a buyer couldn't see their own equipped cosmetics; it now returns `items[]` with `equipped` booleans plus a resolved `equipped{}` object. **Booster fixes:** Double Down now doubles coins AND XP via a distinct `coin_xp_multiplier` effect (no collision with Coin Rush / XP Surge in `active_boosters`), the Streak Shield 3-pack now grants 3 uses (was 1), and Mastery Hint Pack + Voice Skin are marked coming-soon (no consumer/feature yet) and blocked server-side instead of selling dead items. **Reprice:** the entire Fang catalog was repriced upward on the data-economist model (~450/day free income; commons ~2 days, rares ~5 days, epics ~2 weeks, legendaries ~4 weeks, premium flex 16k-45k) — frames 800/2200/5000/13000, banners 700/2300/5200/15000, username effects 16k-45k, animated banners 26k-42k. `app/shop/page.tsx` now reads ids + prices from the canonical `@lionade/core/constants/shop-catalog` (single source of truth) so displayed price equals charged price and every shown id resolves and renders, replacing a stale hardcoded mirror with wrong prices + orphan ids. Migration `lib/migrations/063_equipped_cosmetic_slots.sql` adds the four slot columns + a column-level UPDATE REVOKE from `authenticated`/`anon` (service-role equip route only) — NOT yet applied, Sam runs it manually; until then equip/render is inert on prod and the direct-PATCH defense is not yet active, though the equip routes are safe regardless via the service role. Poker Face secret columns were already whole-table REVOKE'd by migration `056`, so no new security migration there) — 2026-06-11
- **Lion's Den shop** (Coin Store + Premium Store toggle) — 2026-03-06
- **Shop API routes** (purchase, equip, activate-booster) — 2026-03-06
- **Booster integration into quiz flow** — 2026-03-06
- **Wallet/rewards page** — 2026-03-06
- **Whole-surface tier tinting on every Shop card** (each rarity — common/rare/epic/legendary — reads from across the room via a 135° two-layer wash + tier-colored 1.5px border + modest outer glow + 2px left-edge accent stripe in the tier color. Legendary cards additionally carry a 7s gold-luminance sweep across the card surface, mirroring the iOS Go-Pro pill marquee. Reduced-motion respected. Migration of all 10 RARITY_COLORS consumer sites: ConfirmModal, FeaturedCard, CosmeticCard, BoosterCard, InventoryItem (green-override when equipped), PremiumCard, UsernameEffectCard, PremiumFangBannerCard, Today's Drops, Trending. Same pattern as Word Banks confidence rows + Sketchy difficulty picker.) — 2026-06-05
- **Full-width "void-fix" redesign of 5 pages** (web-only layout polish; staged not committed. Fixed the recurring narrow-centered-container "lonely card in a void" problem across the 5 worst audit offenders, bringing them to the `/compete` quality bar. **`/wallet`** widened `max-w-4xl`→`max-w-6xl` into a 2-col bento: LEFT hero balance card with `CountUp` + a balance-trend sparkline derived from the existing `coin_transactions` ledger + XP/Level/Streak stat rail + Redeem; RIGHT a transaction ledger filling the column with per-type left accent bars (green rewards / red spends / purple Ninny) and staggered row reveals; no-flash-of-zero gating on `coins !== null`. **`/games`** MENU view only (in-game screens untouched) → `max-w-6xl` 2×2 ticket grid, lion crest promoted to a right-side header hero, hover-lift + foil-sheen, footer stat strip (pulls-remaining-today / lots-open). **`/quiz` results phase** (mid-quiz UI untouched) → `max-w-4xl` 2-col debrief: score tiles + accuracy ring LEFT, review-mistakes panel RIGHT (with a FLAWLESS-RUN empty-state on perfect scores), score colored by performance tier with a radial glow. **`/learn/paths`** → `max-w-6xl` 2-col subject cards (animated progress rings + stage count + stars + gradient wash) + an Overall Progress summary widget. **`/learn/mastery`** → `max-w-6xl` 2-col: create funnel LEFT (gold focus glow + purple parse-sweep + slide-in result), targets list / "how Mastery works" explainer RIGHT; `classIdContext` breadcrumb preserved. New shared `components/AmbientOrbs.tsx` glow-orb background + GPU-only CSS primitives in `globals.css`, all reduced-motion guarded. Presentation/animation only — no data or logic changes. iOS parity: low-priority / N/A (desktop-gutter artifact; iOS already single-column).) — 2026-05-28

## Profile & Badges
- **Profile page** with overview, badges, stats, quiz history — 2026-02-20
- **Profile Notifications + Privacy server-backed** — 7 notification toggles (Daily Reminder, Duel Challenges, Weekly Report, Badge Unlocked, Streak at Risk, New Features, Promotions) + 4 privacy sub-flags (Show on Leaderboard, Show Streak, Show Fangs, Allow Duels From: everyone/nobody) persist to `profiles.preferences` JSONB via PATCH `/api/user/preferences`. Profile Visibility (public/private) writes the dedicated `profiles.profile_visibility` column via PATCH `/api/user/profile-visibility`; private profiles are excluded from social search + all three leaderboard ladders server-side. *(Superseded 2026-06-11 by the Settings Overhaul — preferences now live under `/settings/notifications` + `/settings/privacy`, and visibility added a `'friends'` option. See Settings & Preferences.)* — 2026-06-05
- **Delete Account** — Profile > Security > Danger Zone. Type-your-email confirmation modal, server cascades through profiles + every FK via `supabase.auth.admin.deleteUser`. Demo account blocked. *(Superseded 2026-06-11 by the Settings Overhaul Danger Zone — delete now SCHEDULES via `pending_deletion_at = now + 24h` with a cron reaper instead of purging immediately, and adds deactivate + cancel-deletion. See Settings & Preferences.)* — 2026-06-05
- **Badge collection page** — 2026-03-06
- **Avatar picker** (Create/Emoji/Color) — 2026-03-01
- **Username system** (unique check, one change per year) — 2026-03-01
- **Cross-platform avatar sync (iOS)** (iOS resolves web's DiceBear SVG avatar URLs to PNG — rewriting `/svg`→`/png` — and uses the same DiceBear default web uses, so the same profile picture renders on web and iOS instead of falling back to initials; uploaded photos pass through unchanged) — 2026-05-22

## Settings & Preferences
- **Settings Overhaul — 6 route-based sections at `/settings`** (web-only; 8 commits, committed not pushed. The old single page is replaced by `app/settings/[section]/` routes under a shared `app/settings/layout.tsx` — ProtectedRoute + Navbar + SpaceBackground + a 6-section nav rail with the limelight active-state pill + a `PendingDeletionBanner`. The whole surface is **save-on-change**: every toggle/select commits immediately with an inline "Saved ✓" that fades after 2s, no per-field Save button. **Account** (`account/page.tsx`): change username (once-a-year lock from `username_changes`), change password (reauth then `auth.updateUser`), change email (verification), connected accounts (`getUserIdentities`, disconnect guarded to keep another login method), active sessions (last 10 from `user_login_events`), avatar editor (DiceBear seed + color live preview). **Privacy** (`privacy/page.tsx`): profile visibility Public/Friends/Private via `/api/user/profile-visibility`, plus show online status, who can send duel challenges, who can send friend requests, appear on leaderboards, show activity in friends' feeds, show Fang balance — all in `profiles.preferences` JSONB via `/api/user/preferences`. **Notifications** (`notifications/page.tsx`): 4 category cards (Study/Social/Rewards/Product), each notification has In-app (`notifications[]`) + Email (`notifications_email[]`) checkboxes, plus a Quiet Hours toggle + From/To range (`quiet_hours_start`/`quiet_hours_end`, 24h). **Data & Usage** (`data/page.tsx`): storage used (per-item `ninny_materials` delete), quiz history summary, Ninny session count vs limit, Download my data (`/api/user/export` JSON bundle, 24h gate via atomic claim on `preferences.last_export_at`), session history, retention notice. **Subscription** (`subscription/page.tsx`): plan badge + perks, Free upgrade CTA, Pro/Platinum renewal + Stripe portal, real usage bars (active Mastery targets, Ninny sessions this month), AI vocab lookups shown as "tracking coming soon", collapsed plan comparison. **Danger Zone** (`danger/page.tsx`): deactivate account (`deactivated_at`, flips to private, reversed on next login), delete account (now SCHEDULES via `pending_deletion_at = now + 24h`, email-typed confirm + demo block), cancel pending deletion. New routes: `/api/user/export`, `/api/user/sessions`, `/api/user/login-event`, `/api/user/account` (GET + scheduling DELETE), `/api/user/account/deactivate`, `/api/user/account/cancel-deletion`, `/api/cron/reap-pending-deletions` (CRON_SECRET, daily `0 5 * * *`, 500/run cap). New `lib/ua-parse.ts` (dependency-free UA parser). Rate limits: export 3/min, DELETE + deactivate share 5/min. Migration `lib/migrations/060_settings_overhaul.sql` adds `profiles.deactivated_at`/`pending_deletion_at`, widens the `profile_visibility` CHECK to add `'friends'`, creates `user_login_events` (RLS owner-only SELECT + FORCE, service-role insert) — NOT yet applied, Sam runs it manually; until then `/api/user/account` GET 500s but pages degrade gracefully. `CRON_SECRET` must be set on Vercel for the reaper) — 2026-06-11
- **User settings page** (theme, font scaling, compact layout) — 2026-03-04
- **Light/dark theme toggle** — 2026-03-04

## Bounties & Bets
- **Bounty Board** with daily/weekly bounties and claim API — 2026-02-28
- **Daily Bet card** with stake/target picker — 2026-02-28

## Security
- **Rate limiting middleware** (in-memory) — 2026-02-20
- **Input sanitization** (XSS/SQL injection prevention) — 2026-02-20
- **Security headers** (CSP, HSTS, X-Frame-Options) — 2026-02-20

## Admin Console (Internal, staff-only)
- **Role system** (`profiles.role`: user / support / admin; trigger blocks self-promotion via PostgREST; migration `lib/migrations/057_admin_console.sql` — pending manual run) — 2026-06-09
- **Role-gated /admin surface** (layout renders nothing for non-staff + redirects; Admin nav tab appears only for support/admin via `/api/admin/me`; every `/api/admin/*` route re-verifies the role server-side via `requireRole`) — 2026-06-09
- **Dashboard overview** (total users, signups today/week, active 24h/7d, Fangs in circulation with cashable/IAP split; single `admin_dashboard_stats` RPC) — 2026-06-09
- **User search + support profile** (email/username/uuid search via `admin_search_users` RPC; masked emails; profile + auth meta + recent Fang transactions + per-user admin history) — 2026-06-09
- **Audited support actions** (password reset = support+; Fang adjust with required reason, role change, suspend/reinstate via auth ban, raw-email reveal = admin only; every action writes `admin_audit_log`; confirmation modal before anything destructive) — 2026-06-09
- **Audit log viewer** (`/admin/audit`, admin only; who did what to whom + when; action/user filters, 50-row pages) — 2026-06-09
- **/reset-password recovery page** (landing for Supabase recovery links; new-password form via `auth.updateUser`; used by the support reset action and any future forgot-password flow) — 2026-06-09

## Error & Empty States (cross-surface)
- **Error-state house pattern rollout** (DiscoverTab pattern everywhere: skeleton while loading, then ErrorState with retry on fetch failure, then a true empty state only when the fetch succeeded with no data. Applied to: class detail (404/not-found card), academia onboarding gate (error card instead of infinite spinner), classes list, vocab banks, GradeTracker, FlashcardStudy, wallet ledger, PastLobbiesPanel, mastery targets. None of these surfaces claims "you have nothing" on a failed fetch anymore) — 2026-06-09
- **Flash-of-zero sweep** (loading-aware gating so zero never flashes before data arrives: profile badges/activity + history tabs, badges header, social notifs/friends/DM/nudges chip, quiz Quick Stats via a `statsLoaded` flag with a placeholder instead of a punitive 0% at zero quizzes, shop tri-state affordance (neutral until balance known, no "Can't Afford" flash), dashboard bounty skeletons, study-dna all-zero nudge card with Share disabled, leaderboard pinned "You / Unranked" row with CTA, StreakCounter dim outline flame at zero) — 2026-06-09
- **Copy correctness pass** ("coins" replaced with "Fangs" in all user-facing strings: profile sidebar, StatsGrid, quiz stats + results, Blitz subtitle; ReviewQueue shows an empty-bank onboarding state instead of a confetti celebration for a bank with zero words; games "four lots" corrected to "five lots"; study-dna em-dash removal) — 2026-06-09

## Other Pages
- **About page** — 2026-02-25
- **Contact/Help page** — 2026-02-25
- **Privacy Policy** — 2026-02-25
- **Terms of Service** — 2026-02-25
- **Demo page** — 2026-02-25
- **Coming soon landing page** with hidden DevOps gate — 2026-02-21; **retired 2026-05-24** — went fully public: replaced waitlist CTAs with "START STUDYING FREE" → /login button, removed beta-gate password modal + DevOps secret trigger, deleted `/api/beta-gate` route, demoted waitlist to a small footer newsletter form, updated ROADMAP/FAQ to reflect live V1 status. `ComingSoonPage` renamed to `LandingPage`.
- **Pricing page** (Free/Pro/Platinum, monthly/annual toggle, full comparison, FAQ) — 2026-04-24; premium glassy redesign + page-local Lionade WebGL shader — 2026-05-15

---

## Next 5 High-Impact Tasks (Ranked)

1. Build Ninny AI study mode (upload material, generate flashcards/questions).
2. Replace mock duel opponents/questions with real Supabase-backed matchmaking.
3. Move coin/XP awarding to secure server-side RPC with validation.
4. Build out Blitz mode (speed round gameplay).
5. Implement weekly tournament system with bracket and rewards.

## Academia
- **Academia/Study onboarding gate** (required 5-step setup — school type, class count, school name + grade/year, field of study, study intensity — before the Study tab unlocks; web `/academia/onboarding`. iOS parity port `app/academia-onboarding.tsx` with fail-open `useFocusEffect` gate + gold "+ ADD" CTA) — 2026-05-22
- **Academia calendar + agenda + assignment tracker** (new `class_assignments` table — per-class to-dos with a todo/doing/done status, nullable due_date, owner-only RLS — plus assignment CRUD at `GET`/`POST /api/classes/[id]/assignments` and `PATCH`/`DELETE /api/classes/assignments/[assignmentId]`, all owner-scoped, and a new `GET /api/academia/agenda?from&to` feed merging exam target dates (`user_exams.target_date`) and assignment due dates into one date-sorted user-scoped stream with YYYY-MM-DD validation, a <=120-day clamp, and a today..+60 default; standalone Mastery exams (null class_id) get a neutral gold "Mastery" treatment. The Academia hub (`app/academia/page.tsx`) gains a `PlannerSection` above the classes grid: a THIS WEEK agenda (next 7 days grouped by day, inline optimistic assignment status toggle, exams read-only) and a color-coded month calendar (per-class dots, exam ring, today gold ring, prev/next month + Today jump, click-a-day to focus the agenda; one SWR fetch per visible month, range always covers today+7). Class detail (`app/classes/[id]/page.tsx` + `components/Class/AssignmentTracker.tsx`) gains an ASSIGNMENTS section: add/list/complete/delete, due countdowns, overdue-red only when not done, done rows sink + dim, optimistic mutations, skeleton/empty/error states. Migration `supabase/migrations/20260610_class_assignments.sql` applied to prod; zero API cost; web-only) — 2026-06-10
- **Study-time blocking** (a "Build study plan" action on the class detail page backed by `GET`/`POST /api/classes/[id]/study-plan`; the GET proposes study blocks by distributing the exam's weakest `mastery_subtopics` across the days before `target_date`, capped at 21, and the POST bulk-commits the chosen blocks as `class_assignments` so they appear on the calendar/agenda/tracker. A BottomSheet preview lists the proposed blocks with per-block deselect before commit. No migration — reuses `class_assignments`; zero API cost; web-only) — 2026-06-10
- **Cross-class GPA snapshot** (`GET /api/academia/gpa` rolls each class's weighted grade % — same math as the per-class grades route — into a US letter grade + 4.0 grade points + an unweighted term GPA, with credit-weighting noted as a future enhancement; the Academia hub shows a GRADE SNAPSHOT strip with the term GPA + a per-class chip for each graded class. No migration; zero API cost; web-only) — 2026-06-10
- **ICS calendar import** (`POST /api/academia/import-ics` previews `{ url }` then commits `{ classId, events }` as assignments; a hand-rolled RFC 5545 parser with no dependency extracts VEVENT `SUMMARY` + `DTSTART` in all forms, so a Canvas/Google/Outlook/Apple feed imports as class assignments. Heavy SSRF defense: http(s) only, DNS-resolve + block private/loopback/link-local/CGNAT/cloud-metadata for every resolved address incl IPv4-mapped-IPv6, `redirect: manual` with per-hop re-validation, a pinned lookup closing the DNS-rebinding TOCTOU, an 8s timeout, a 2MB cap, a `BEGIN:VCALENDAR` sniff, and generic errors. The Academia hub gets an "Import calendar" BottomSheet: paste URL → preview + per-event select + class picker → import. Rate limited 10/min. No migration; zero API cost; web-only) — 2026-06-10
- **Weekly email digest** (Monday cron `GET /api/cron/academia-digest` with CRON_SECRET bearer auth emails opted-in users their next-7-day exams + assignments via a new Resend template `academia-weekly`; opt-in is `profiles.preferences.notifications.weekly_report != false`, the run skips opted-out and empty-digest users, escapes user strings, and caps 500/run ordered by `created_at`; scheduled in `vercel.json` for Monday 13:00 UTC. A live send only fires on Vercel with both `CRON_SECRET` and `RESEND` configured. No migration; zero API cost; server-side, platform-agnostic) — 2026-06-10
- **Academia hub polish** (the month calendar gains loading + error states — dimmed grid + spinner while loading, a "Couldn't load events" retry overlay on failure — instead of silently rendering an empty grid; the quick-stats "Next exam" tile is replaced by a "Due this week" count off the deduped agenda SWR key; the planner is unified under one PLANNER header with consistent sub-labels + the Import-calendar button; a zero-classes first run collapses to a single "add your first class" CTA with recent-notes hidden until the first class; em-dashes stripped from assignment status tooltips. No migration; zero API cost; web-only) — 2026-06-10
- **Workload heat warning** (a "crunch" banner at the top of the PLANNER section, computed client-side from the already-fetched agenda items with no new endpoint; a 3-day window slides over the next 14 days and surfaces the densest/soonest window with `>=3` due items — "Heads up: 4 things due Jun 14 to Jun 16. Start early." — with a 2-exams-plus-2-assignments sub-line, gold caution at 3 items and red heat at 4+, gated on resolved data and hidden when there's no crunch. No migration; zero API cost; web-only) — 2026-06-10

## Navigation
- **iOS bottom-tab parity with web nav** (5 tabs in web-parity order: Home · Academia · Learn · Compete · Social. "Study" renamed to "Academia"; Learn promoted from a pushed Stack screen to a top-level tab; "You" dropped from the tab bar but `/profile` deep-links preserved via `href: null` — the rich profile hub is now reached by tapping the avatar in the profile side panel header. Limelight pill height bumped 58 → 64 with a ±3 translateX + +6 width grow on edge cells so Home and Social hug the bar's rounded inner curve; active pill `top: 3` so it sits vertically centered in the 70pt bar — 2026-05-23) — 2026-05-23

## Navigation
- **Consistent route-based back affordance** (semantic-parent "Back to {Parent}" on every non-root screen, web + iOS; roots/funnels excluded by design) — 2026-05-15
- **Sliding "limelight" bottom-nav highlight** (single travelling gold pill springs to active tab; framer-motion shared-layout on web, Reanimated spring on iOS; reduced-motion → instant; existing gold tokens reused — zero design drift) — 2026-05-19
- **Full-screen edge-swipe-back (iOS)** (`fullScreenGestureEnabled` on the navigation Stack — iOS back-swipe works from anywhere across the screen, not just the ~20px left edge) — 2026-05-22

## Dashboard (iOS)
- **Tappable stat orbs** (all 5 dashboard orbs — Fangs, Streak, Level, Subjects, Rank — open a blurred-backdrop detail window: Fangs balance + View Wallet, current/best streak + next milestone + how-it-works, XP-to-next + progress bar, per-subject accuracy list, View Leaderboard) — 2026-05-22
- **Profile hub side panel** (tapping the top-left avatar opens a left slide-in drawer with blurred backdrop. Avatar with an animated tier-colored breathing neon ring (reduce-motion respected) + tier badge + Fangs balance chip; a gradient "Go Pro" card (dormant PRO-member badge); a 2×3 shortcuts grid — Shop, Wallet, Badges, Leaderboard, Games, Study DNA; Account rows — Edit Profile, Settings, Notifications; More rows — Invite a Friend, Rate Lionade, Help & Support, Privacy & Terms. Rebuilt from the build-9 drawer, fixing label-less rows) — 2026-05-22; polished into a fully-rounded Go-Pro pill with a 7s seamless marquee gradient (gold→purple→electric, reduce-motion respected), 5 shortcut tiles in a centered 2-2-1 grid (Edit Profile · Settings / Notifications · Invite a Friend / Rate Lionade), Help & Support + Privacy & Terms demoted to dim text links above Sign out, and a curved right edge (48px corner radius with a right-edge shadow via a two-layer surface so iOS still draws shadow under the clip) — 2026-05-23; polish v3 — Go-Pro pill marquee now loops byte-identically (7-stop palette with GOLD anchors at 0 / 0.5 / 1.0, no visible refresh), Go-Pro + PRO-MEMBER pills restructured into a two-layer construct (outer paints shadow, inner clips with `overflow: hidden` + `borderRadius: 999`) finally killing the "colors going outside" bleed, and pills are `maxWidth: 300` centered with reduced shadow opacities so the purple glow no longer reads as color leak — 2026-05-23
- **Blurred-backdrop bottom sheets** (shared `Sheet` component blurs the app behind it via `expo-blur` with a hairline-bordered floating-card edge; Quick Note capture sheet capped below full-screen to read as a floating window) — 2026-05-22

## Profile (iOS)
- **Profile tab → full settings hub** (the "You" tab rewritten as one long scrollable Apple-HIG inset-grouped page: hero + stat strip on top, then Lifetime / Recent Badges / Stats & Rankings / Account / Subscription / Appearance / Notifications / Privacy / Recent Activity / Support / About / Sign out + Delete account. Every section from web's `/settings` is mirrored inline; toggles share the same Supabase `user_preferences` row + AsyncStorage keys as `/settings` so changes round-trip across screens) — 2026-05-23
- **App-icon variant gating (iOS)** (Midnight / Wildfire / Platinum / Void variants show "SOON" tags + lock overlays + a "Coming Soon" alert on tap, instead of pretending to apply — variant artwork + native icon swap aren't shipped yet) — 2026-05-23

## Settings & Preferences (iOS)
- **Cross-platform onboarding-sync** (iOS auth gate `lib/auth-context.tsx` now also treats a profile as onboarded if `selected_subjects` or `education_level` is present — onboarding-only data, OAuth can't auto-derive — and self-heals by backfilling `onboarding_completed=true`. Web-onboarded accounts no longer get re-prompted on iOS; Apple/Google fresh signups still flow through onboarding correctly) — 2026-05-23

## Learn (iOS)
- **Learn tab — web-parity close-out** (primary CTA deep-links to weakest subject via `/quiz?subject=…`; mastery rows route per-subject; 7-day activity heatmap gained a legend under the grid — less → 5 gold swatches → more — with a gold glow on today's cell; Level stat chip shows the lowercase tier name below the number; RECENT section header gained a tappable `new →` link that starts a fresh quiz) — 2026-05-23

## Compete (iOS)
- **Compete tab restructured** (Top Players mini-leaderboard removed from Compete; replaced with a single "Leaderboard" row at the bottom of the Modes group that routes to `/leaderboard`. `GroupedList`'s `BlurView` wrapper — which was collapsing intrinsic widths and pushing `ModeRow` chevrons below — replaced with a plain matte `View` fill; `ModeRow` labels switched to the safe `Inter` font, chevron is the last sibling after the flex-1 label View, rows have uniform `minHeight: 56`) — 2026-05-23
- **Top-20-with-anchor leaderboard view** (iOS leaderboard screen now renders the top 20 — podium 1–3 + list 4–20 — and if the viewer is outside top 20 their rank is anchored at the bottom in a gold-tinted highlight; covers ranks 21–60 from the fetched entries, with a fallback to absolute rank + profile stats if outside top 60. `useLeaderboard` hook bumped from top-50 → top-60 fetch and now exposes `userRank: number | null`) — 2026-05-23

## Bounties & Bets (iOS)
- **Daily Bet relocated to Dashboard** (web parity — web does not host Daily Bet on Compete. The card now sits inside the Today section between Bounties and the Progress label, returning to its pre-2026-05-13 home) — 2026-05-23
